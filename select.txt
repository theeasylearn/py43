1) select all fields & all rows from customers table 
SQL : select * from customers

2) select customerNumber, customerName, city, state, country of all customers from customers table 
SQL : select customerNumber, customerName, city, state, country from customers

3) select 1st 50 customers' name, city, state & country from customers
SQL : select customerName,city,state,country from customers limit 0,50

4) select next 50 customers' name, city, state & country from customers
SQL : select customerName,city,state,country from customers limit 50,50

select next 100 customers' name, city, state & country from customers
SQL : select customerName,city,state,country from customers limit 100,100

display data in specific order 
5) customerNumber, customerName, city, state, country of all customers from customers table in name wise ascending order 
SQL : select customerNumber, customerName, city, state, country from customers order by customerName

6) customerNumber, customerName, city, state, country of all customers from customers table country wise then state wise and then city wise in ascending order 
SQL : select customerNumber, customerName, city, state, country from customers order by country, state, city 

6) customerNumber, customerName, city, state, country, creditLimit of all customers from customers table creditLimit wise in descending order 

SQL : select customerNumber, customerName,creditLimit, city, state, country from customers order by creditLimit desc 

filter data (fetch data as per conditions)

7) select customerNumber, customerName, city, state, country, creditLimit of those customers whose country is USA
SQL : select customerNumber, customerName, city, state, country, creditLimit from customers where country='USA'

8) select customerNumber, customerName, city, state, country, creditLimit of those customers whose city  is Boston
SQL : select customerNumber, customerName, city, state, country, creditLimit from customers where city='boston'

9) select customerNumber, customerName, city, state, country, creditLimit of those customers whose country is USA or France 
SQL : select customerNumber, customerName, city, state, country, creditLimit from customers where country='USA' or country='France'

10) select customerNumber, customerName, city, state, country, creditLimit of those customers whose country is USA or France or Australia or japan
SQL : select customerNumber, customerName, city, state, country, creditLimit from customers where country in ('USA','france','Australia','japan')
